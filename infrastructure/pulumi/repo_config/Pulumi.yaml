name: scaffold-test-3053-repo-config
runtime: go
description: Pulumi IaC that manages this repository's own GitHub settings (auto-delete merged head branches + parameterized branch protection on the default branch).
